<div id="wrapper">
        <div class="row">
            <div class="col-md-12 text-center">
                <h1 class="text-center">HBR Scraper</h1>
                <h2 class="text-center"><i>Harvard Business Review Articles</i></h2>
            </div>
        </div>
        
        <div class="row">
            <div class="col-md-12 text-center">
                <a href="/scrape"><button class='btn btn-success text-center' id='scrapeIt'>Scrape and View Articles</button></a>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <ul class="list-group">
                    {{#each articles}}
                        {{> articles/article-block}}
                    {{/each}}
                </ul>
            </div>
            <div class="col-md-12">
                {{!-- <div class="container d-flex flex-column">
                <div class="container" id="commentContainer">
                    <p class="text-center">Notes</p>
                    <table class="table" id="noteTable">
                    </table>
                </div>
                <div class="container d-none" id="newNote">
                    <form id="newNoteForm">
                        <div class="form-group">
                            <label for="formUser">User</label>
                            <input type="text" class="form-control" id="formUser" placeholder="John Smith">
                        </div>
                        <div class="form-group">
                            <label for="formNote">Add your note here</label>
                            <textarea class="form-control" id="formNote"></textarea>
                        </div>
                        <button type="submit" id="noteSubmit" class="btn btn-danger">Submit</button>
                    </form>
                </div>
            </div>
 --}}

                <div id="notes"></div>
            </div>
        </div>
</div>